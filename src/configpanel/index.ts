// Webpack entry point — the panel is loaded via Module Federation, so
// this file is only here to give webpack a module-graph anchor. The
// actual React component is exposed under `./PluginConfigurationPanel`
// in `webpack.config.js`.
export {}
